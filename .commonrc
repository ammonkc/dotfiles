# Load things
[[ -f "$HOME/.exports" ]] && source "$HOME/.exports"

# Aliases
[[ -f "$HOME/.aliases" ]] && source "$HOME/.aliases"

# Functions
[[ -f "$HOME/.functions" ]] && source "$HOME/.functions"

# Prompts
[[ -f "$HOME/.prompts" ]] && source "$HOME/.prompts"

# Rackspace novaclient
[[ -f "$HOME/.novarc" ]] && source "$HOME/.novarc"
