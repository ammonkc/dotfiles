# XDG-compliant npm configuration
# This file is managed by dotfiles - edit the source in ~/.dotfiles/packages/npm/

# Cache directory (prevents ~/.npm/_cacache)
cache=${XDG_CACHE_HOME}/npm

# Logs directory (prevents ~/.npm/_logs)
logs-dir=${XDG_STATE_HOME}/npm/logs

# Global packages prefix
prefix=${XDG_DATA_HOME}/npm

# Disable update notifier (prevents ~/.npm/_update-notifier-last-checked)
update-notifier=false
