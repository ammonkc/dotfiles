# This is copied on install and not overwritten on subsequent calls to install.
# This is where to put:
#   local environment variables

# Homebrew GitHub API token (reads from git config for single source of truth)
if _token=$(git config --get github.token 2>/dev/null); then
  export HOMEBREW_GITHUB_API_TOKEN="$_token"
fi
unset _token
